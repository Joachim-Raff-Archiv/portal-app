<project default="xar" name="raffArchive">
    <xmlproperty file="expath-pkg.xml"/>
    <property name="project.version" value="${package(version)}"/>
    <property name="project.app" value="raffArchive"/>
    <property name="build.dir" value="build"/>
    <tstamp/>
    <target name="xar">
        <mkdir dir="${build.dir}"/>
        <zip basedir="." destfile="${build.dir}/${project.app}-${project.version}-${DSTAMP}-${TSTAMP}.xar" excludes="${build.dir}/*"/>
    </target>
</project>