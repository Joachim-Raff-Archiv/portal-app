#
# Don't directly modify this file.
# Instead, copy it to local.build.properties and edit that.
#
# general
project.app=raffArchive
project.name=Joachim Raff Archive App
project.url=https://github.com/Joachim-Raff-Archiv/portal-app
project.version=1.3.0
project.status=production

release.date='2025-12-11'
release.year=2025

wega-webapp-lib-version=1.12.0

# build output dirs
build.dir=build
dist.dir=dist

# engines
bash.command=/bin/bash
git.command=/usr/bin/git
